[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools>=45", "setuptools_scm[toml]>=6.2", "wheel"]
[project]
name = "sleakops"
description = "A CLI tool to build and deploy projects with Sleakops"
readme = "README.md"
dynamic = ["version"]
[tool.setuptools_scm]