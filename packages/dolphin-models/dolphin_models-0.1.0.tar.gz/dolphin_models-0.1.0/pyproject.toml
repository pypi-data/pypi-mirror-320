[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dolphin_models"
version = "0.1.0"
description = "A Python package implementing popular machine learning models."
authors = [{ name = "Surieya", email = "surieyasurieya@gmail.com" }]
license = { file = "LICENSE" }
requires-python = ">=3.6"
