from sigmap.sigmapred import SigmaFactor
from sigmap.setup import readseq, fasta2df

# hatch version patch
# or hatch version minor
# or hatch version major

# hatch build
# hatch publish --user __token__ -a <your_api_key>
__version__ = '1.1.2'