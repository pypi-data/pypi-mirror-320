[build-system]
requires = ["poetry>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "python203"
version = "0.1.2"
description = "Python project as part of the Python Programming course taught by Juan Imbet in the Master 203 program at Paris Dauphine-PSL University."
authors = ["Maxime <max70990@gmail.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = ">=3.8"
typer = "^0.4.0"

[tool.poetry.dev-dependencies]
coverage = "^6.0"
mypy = "^1.14.1"
pytest = "^7.0"
ruff = "^0.1.0"
