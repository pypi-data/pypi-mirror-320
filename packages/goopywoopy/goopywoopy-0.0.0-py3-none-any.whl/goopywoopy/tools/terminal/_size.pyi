import shutil

def terminal_width() -> int:
    """Get current terminal window width (number of characters
    that can be printed without line break)."""

def terminal_height() -> int:
    """Get current terminal window height (number of lines that
    can be printed without triggering scroll)."""
