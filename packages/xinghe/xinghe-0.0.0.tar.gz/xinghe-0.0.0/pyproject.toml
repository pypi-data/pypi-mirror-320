[project]
name = "xinghe"
version = "0.0.0"
description = "Default template for PDM package"
authors = [
    { name = "opendatalab", email = "opendatalab@pjlab.org.cn" },
]
dependencies = []
requires-python = ">=3.8"
readme = "README.md"

[project.license]
text = "MIT"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool.pdm]
distribution = true
