[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "minivectordb-simple"
version = "1.0.0"
authors = [
  { name="Carlo Moro", email="cnmoro@gmail.com" },
]
description = "A python project aimed at extracting embeddings from textual data and performing semantic search. This version supports only the basic onnx model, no bge-m3 or e5 models."
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = [
    "numpy<2",
    "faiss-cpu",
    "pytest",
    "pytest-cov",
    "scikit-learn",
    "thefuzz[speedup]",
    "onnx",
    "onnxruntime",
    "onnxruntime-extensions",
    "peft",
    "usearch"
]

[project.urls]
"Homepage" = "https://github.com/cnmoro/Mini-Vector-DB"
"Bug Tracker" = "https://github.com/cnmoro/Mini-Vector-DB/issues"