[project]
name = "dsmq"
version = "1.0.0"
description = "A dead simple message queue"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "websockets>=14.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
dsmq = { workspace = true }
