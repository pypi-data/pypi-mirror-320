[project]
name = "python-practice-algorithm-pack"
version = "0.1.0"
description = "A project for practicing python packaging techniques."
authors = [
    {name = "Dimitar Stoyanov",email = "me@dimitars.dev"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.group.test.dependencies]
pytest = "^8.3.4"
pytest-cov = "^6.0.0"


[tool.poetry.group.dev.dependencies]
black = "^24.10.0"
mypy = "^1.14.1"
pylint = "^3.3.3"

