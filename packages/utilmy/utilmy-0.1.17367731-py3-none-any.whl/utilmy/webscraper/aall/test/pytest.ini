[pytest]
norecursedirs = _build src*
python_files = *.py
#python_classes = Check
#python_functions = *_check

# minversion = 6.0
        # show summary of skipped tests

addopts =
         --ignore  test/utils/utilmy_redis.py
         --ignore  test/utils/utilmy_aws.py
         --ignore  test/utils/utilmy_parallel.py
         --ignore  test/simulation/simulation.py
         -k " not _aws_   and not test_all "
;        -rsv
;        --cov=src/
;        --cov-config=.coveragerc
;        --cov-report term-missing

xfail_strict = False

testpaths =
    tests

#filterwarnings =
#    # make warnings into errors
#    error

markers =
    src: Dependency on src