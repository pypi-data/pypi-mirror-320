from setuptools import setup, find_packages

VERSION = "0.1"  # PEP-440

NAME = "mmd-useful"

INSTALL_REQUIRES = []

setup(
    name=NAME,
    version=VERSION,
    packages=find_packages(),
    INSTALL_REQUIRES=[]
)