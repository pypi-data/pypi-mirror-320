[tool.poetry]
name = "pvl_sdk"
version = "0.1.0"
description = "Python SDK for FastAPI backend"
authors = ["Narasimman Rajendran <your-email@example.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.8"
requests = "^2.26"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
