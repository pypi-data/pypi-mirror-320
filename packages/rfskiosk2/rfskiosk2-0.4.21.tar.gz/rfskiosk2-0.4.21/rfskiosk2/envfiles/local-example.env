# This allows system defaults to be overridden if needed (rarely necessary, except in test env or multiple kiosks)
# Brigade name normally same as PI HOST name. Override here if that's not the case.
#KIOSK_BRIGADE=mybrigade
# Default location is 'station'. If multiple kiosks are present, they should all have different location names.
#KIOSK_LOCATION=home
# Production server rfstag.com is default. Anything else is for testing purposes only.
#BUSHFIRE_SERVER=rfstag.org
# Basic INI details
NFC_INI_FILE=/home/pi/.config/rfstag/nfcreader.ini
