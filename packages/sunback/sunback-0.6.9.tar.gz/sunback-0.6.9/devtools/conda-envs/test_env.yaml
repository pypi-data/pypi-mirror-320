name: test
channels:
  - conda-forge

dependencies:
    # Base depends
  - python
  - pip
  - astropy
  - pathlib
  - numpy
  - sunpy[net]
  - pillow

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
      - ctypes
      - urllib
      - time
#      - mpi4py

#    - codecov

