[build-system]
# 3.4 이상이어야 pip install -e 가 가능하다
requires = ["flit_core >=3.7"]
build-backend = "flit_core.buildapi"

[project]
name = "django_medilab_dental"
version = "5.0.7"
description = "Demiansoft template(medilab)"
readme = "README.md"
requires-python = ">=3.6"
#license = {text = "MIT"}
#authors = [
#    {name = "Hyungjin Kim", email = "hj3415@gmail.com"}
#]
#keywords = ["keyword1", "keyword2", "keyword3"]
#classifiers = [
#    "Programming Language :: Python :: 3",
#    "License :: OSI Approved :: MIT License",
#    "Operating System :: OS Independent"
#]
dependencies = [
    "Django>=4.2.11",
    "shared_lib_ds>=1.3.3"
]

[tool.flit.module]
name = "medilab"  # 배포 패키지 이름과 모듈 이름이 다른 경우. 3.7이상에서 사용
