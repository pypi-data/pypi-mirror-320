[build-system]
requires = ["setuptools", "wheel", "ruff"]
build-backend = "setuptools.build_meta"

[tool.ruff]
line-length = 90
indent-width = 4

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]

fixable = ["ALL"]

[tool.ruff.format]
quote-style = "double"
indent-style = "tab"
