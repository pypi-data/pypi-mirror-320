[project]
name = "TubeKit"
version = "0.1.5"
description = " A simple Client API for YouTube"
authors = [
    { name = "F4k3r22/Fredy Rivera", email = "riveraaai200678@gmail.com" }
]
requires-python = ">=3.8"
dependencies = [
    "requests",
    "isodate",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["TubeKit"]