[project]
name = "sysgit"
version = "0.1.0"
description = "When the internal logic for this package is implemented, more detail will be added."
readme = "README.md"
requires-python = ">=3.10,<=3.13"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["sysgit"]
