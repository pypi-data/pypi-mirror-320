[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ccompass"
version = "0.0.0a0"
description = "The C-COMPASS package"
authors = [
    #{ name = "Your Name", email = "your.email@example.com" }
]
dependencies = []
