[tool.poetry]
name = "beholder_client"
version = "0.2.1"
description = "Python client library for beholder."
readme = "README.md"
license = "MIT"
repository = "https://github.com/mbari-org/beholder-py"
authors = ["Kevin Barnard <kbarnard@mbari.org>"]

[tool.poetry.dependencies]
python = "^3.9"
requests = "^2.28.1"
Pillow = ">=9.2.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
