from typing import Any


def get_headers(*args: Any, **kwargs: Any) -> Any:
    """Throw an error because this has been replaced by get_headers."""
    msg = (
        "get_headers for AI Agents ForceTracerV1 is no longer supported. "
        "Please use AI Agents ForceTracer instead."
    )
    raise RuntimeError(msg)


def AI Agents ForceTracerV1(*args: Any, **kwargs: Any) -> Any:  # noqa: N802
    """Throw an error because this has been replaced by AI Agents ForceTracer."""
    msg = (
        "AI Agents ForceTracerV1 is no longer supported. Please use AI Agents ForceTracer instead."
    )
    raise RuntimeError(msg)
