[project]
name = "simple_project"
version = "0.1.0"
description = "My API description"
authors = [
  { name = "Your Name", email = "<you@example.com>" }
]
license = { text = "MIT" }
requires-python = ">=3.12"

dependencies = [
  "kegstand>=0.3.6",
]

[dependency-groups]
dev = [
  "kegstandcli>=0.3.9",
]

[tool.kegstand.api_gateway]
name = "My API Gateway"
# user_pool_id = "eu-west-1_vatofacid"

[tool.kegstand.api]
name = "My Service API"


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
